apiVersion: v1
kind: Pod
metadata:
  labels:
    run: pod-giropops
  name: pod-giropops
spec:
  containers:
  - image: nginx
    name: pod-giropops
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
